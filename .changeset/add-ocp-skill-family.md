---
"atpaawej-skills": minor
---

Add OCP skill family — /design-ocp (greenfield) and /improve-ocp (retrofit). Design closed core, extension points, and agent contract so every new feature is a new file. Includes shared OCP glossary, templates, and 7-signal violation scanner.

Restructure repo into skills/ container with categories (engineering/, project/) matching the upstream convention.
