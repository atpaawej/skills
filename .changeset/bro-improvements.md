---
"atpaawej-skills": minor
---

Improved /bro: rewrote the flow section into six phases each ending on a checkable completion criterion; added a conversation map (root / frontiers / trail) so the session has a tracked state instead of wandering; added explicit mechanics for how a conversation starts when invoked empty; operationalized "one point at a time" as a checkable rule; turned "both sides have opinions" into a turn rhythm where the agent states a position and asks for yours before moving on; replaced the vague "when useful" with three concrete recommendation triggers; added a tangent boundary (three-turn rule); rephrased the "what it is not" and "when to use" blocks to remove negations; tightened leading words to match the new machinery. Same skill, same name, same voice — just made the conversation reliable.
